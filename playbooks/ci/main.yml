---
- name: Configure CI server
  hosts: ci-server
  vars_files: 
    - vars/vars.yml
    - vars/vault.yml
  become: yes

  roles:
    - ansible
    - jenkins

  tasks:
    - include: jenkins-config.yml
    - include: docker-registry.yml
